#include <bits/stdc++.h>

using namespace std;

int main() {
	ios_base::sync_with_stdio(false), cin.tie(nullptr);

	int tt; cin >> tt;
	for (int i = 1; i <= tt; i++) {
		for (int j = 1; j <= tt; j++) {
			cout << i << ' ' << j << '\n';
		}
	}

	return 0;
}
