#include <bits/stdc++.h>

using namespace std;
using ll = long long;

const int N = 100010;

void solve() {
    bool t = false;
    cout << (t ? "How are you?" : "I'm fine Thank you") << endl;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T = 1;
    // cin >> T;

    while(T--) {
        solve();
    }

    return 0;
}
