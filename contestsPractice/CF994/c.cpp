#include <bits/stdc++.h>

void solve() {
    int n, x, y;
    std::cin >> n >> x >> y;
    x--, y--;
	
}

int main() {
    std::ios_base::sync_with_stdio(false), std::cin.tie(nullptr);

    int T; 
    std::cin >> T;

    while (T--) {
        solve();
    }

    return 0;
}
